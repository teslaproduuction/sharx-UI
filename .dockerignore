# Git
.git
.gitignore
.github

# Documentation
*.md
!README.md
docs/
CONTRIBUTING.md
ENV_VARIABLES.md

# Development files
.vscode
.idea
*.swp
*.swo
*~

# Logs (exclude log files but keep source code)
*.log
logs/*.log
node/logs/*.log

# Database
*.db
*.sqlite
*.sqlite3
db/

# Certificates (should be mounted as volumes)
cert/
node/cert/

# Data folders (should be mounted as volumes)
data/
node/data/
bin/
node/bin/

# Docker compose files (not needed in build context)
docker-compose.yml
docker-compose.*.yml
node/docker-compose.yml
node/docker-compose.harbor.yml

# Scripts (keep only what's needed for build)
push-harbor.sh

# Media files (not needed in container)
media/

# Windows files
windows_files/

# Baked by panelui stage — keeps Docker context small; panel static comes from multi-stage copy
web/panel/

# Next.js noise (panel source still copied; these need not enter context)
panel/.next/
panel/out/
panel/node_modules/
panel/tsconfig.tsbuildinfo

# Build artifacts
build/
dist/
*.exe

# Test files
*_test.go
test/

# Temporary files
tmp/
temp/
*.tmp
