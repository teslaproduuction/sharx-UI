## Release v1.1.7

**Release Date:** April 27, 2026

Патч-релиз относительно **v1.1.6**: проверка обновлений в панели.

### Исправления

- **Индикатор обновления / `appMeta`:** запрос последнего GitHub-релиза переведён с репозитория `konstpic/sharx-code` на **[konstpic/SharX](https://github.com/konstpic/SharX/releases)** — там публикуются пользовательские релизы и заметки; версия и ссылка «есть обновление» в шапке панели теперь соответствуют этому каналу.

### 🐳 Docker Images

Images are available in Harbor:

- **sharx**: {{SHARX_TAG_VERSION}} / {{SHARX_TAG_LATEST}}
- **sharxnode**: {{SHARXNODE_TAG_VERSION}} / {{SHARXNODE_TAG_LATEST}}
- **postgres**: {{HARBOR_HOST}}/{{HARBOR_PROJECT}}/postgres:16-alpine / {{HARBOR_HOST}}/{{HARBOR_PROJECT}}/postgres:latest

### 📦 Upgrade from v1.1.6

1. Сделайте **бэкап** PostgreSQL и `.env`.
2. Подтяните образы **`1.1.7`** / **`latest`** и пересоздайте контейнер панели.

```yaml
services:
  sharx:
    image: {{SHARX_TAG_LATEST}}
  sharxnode:
    image: {{SHARXNODE_TAG_LATEST}}
  postgres:
    image: {{HARBOR_HOST}}/{{HARBOR_PROJECT}}/postgres:latest
```

Полный список изменений — `git log` / diff от тега `v1.1.6`.
